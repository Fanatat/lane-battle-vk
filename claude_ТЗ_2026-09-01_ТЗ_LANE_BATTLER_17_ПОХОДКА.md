# ТЗ №17 — ЖИВАЯ ПОХОДКА: СУСТАВ ВМЕСТО СНОСА ТОЧКИ

Дата: 2026-09-01. Ветка `prototype/battle`, поверх ТЗ №15/16 (rig принят,
силуэт залит, глаза были частью лица). Мастер-спека — `CLAUDE.md`.

**Вердикт основателя (дословно, повод для этого ТЗ):** «первое ощущение,
это не человекоподобная анимация... нужно чтобы туловище наклонялось при
беге, чтобы руки сгибались в локтях и ноги сгибались в колене». Отдельным
решением в этой же сессии: **лица у человечков больше нет** — голова
сплошная заливка кругом, глаза не рисуются. Это отменяет ТЗ №16 п.1.2
(«глаза — единственная деталь лица») для этого рига.

---

## 1. ДИАГНОЗ — почему сейчас «не человекоподобно»

Причина не в стиле фигуры, а в математике сустава. В `computeRig()`
(`rig.js:65-155`) колено и локоть — это НЕ сустав, а точка, которая всегда
сидит на фиксированной высоте:

```
kneeDrop = legLen * 0.52;                 // rig.js:122 — константа, не зависит от фазы
legL: { kx: cx - hipSpread + legSwingA * 0.5, ky: hipY + kneeDrop, ... }  // rig.js:150
```

`ky` (высота колена) не меняется НИКОГДА — колено просто «едет» по
горизонтали вслед за стопой (`legSwingA`), оставаясь на одной и той же
высоте относительно бедра. Физически это не сгиб, а жёсткая двухзвенная
палка, которая транслируется вбок. То же для локтя (`elbowDrop`,
`rig.js:120,152-153`).

Наклон корпуса (`lean`, `rig.js:82`) вообще НЕ задействован в режиме
`walk` — переменная `lean` меняется только в `attack` (`rig.js:99`), при
ходьбе она всегда 0 (`rig.js:82`, инициализация, ветка `walk` её не
трогает: `rig.js:86-92`). Поэтому корпус на бегу стоит вертикально столбом
— то, что и увидел основатель.

Вывод: чинить нужно не силуэт (он уже принят), а САМУ ФОРМУЛУ походки —
заменить «снос точки по X при неизменном Y» на настоящий сгиб сустава, и
добавить наклон корпуса в режиме `walk`, которого там сейчас нет вообще.

## 2. ЧТО ДЕЛАТЬ

2.1. **Колено/локоть — реальный сгиб.** Между бедром/плечом и
     стопой/кистью — двухзвенная кинематика (2-bone IK), не константная
     высота сустава. Точная реализация — на усмотрение исполнителя;
     проверенный в этой сессии подход (см. раздел 3) — сустав ровно на
     середине хорды «начало→цель» со сдвигом перпендикулярно хорде на
     `h = 0.5·√(len² − d²)` (упрощение для равных по длине сегментов:
     бедро=голень, плечо=предплечье), `d` — расстояние до цели. Сгиб
     всегда в одну и ту же сторону (по направлению наклона корпуса) —
     колени/локти не гнутся «то вперёд, то назад» от кадра к кадру.
     **Важно: сторона сгиба у колена и у локтя — ПРОТИВОПОЛОЖНАЯ.**
     Колено гнётся ВПЕРЁД (в сторону наклона корпуса) — так и должно
     быть, это анатомически верно. Локоть гнётся НАЗАД, к туловищу —
     если применить тот же знак смещения, что у колена, острый угол
     локтя торчит наружу-вверх («в небо», от тела), а не к корпусу. В
     прототипе (раздел 3) это заведено как противоположный `sign` в
     одной и той же функции сустава — не разные формулы, один и тот же
     `twoBoneJoint`, только знак перпендикуляра у руки инвертирован
     относительно ноги (правка 2026-09-01 по замечанию основателя:
     «острый угол смотрит в небо... должен смотреть на туловище»).
2.2. **Стопа отрывается от земли только в фазе выноса**, не весь цикл —
     иначе исчезает ощущение шага, остаётся скольжение. Признак фазы
     выноса — стопа движется ВПЕРЁД относительно бедра, а не назад
     (в терминах текущей `phase`: `cos(phase) > 0`).
2.3. **Наклон корпуса вперёд на ходу** — сейчас `lean` при `walk` == 0.
     Небольшой фиксированный угол вперёд (не рывками, не по синусу —
     именно наклон, ровно то слово, что употребил основатель). Голова и
     плечи следуют за наклоном торса как единая палка от таза — таз
     остаётся опорной точкой.
2.4. **Контралатеральность сохраняется** — рука уже сейчас движется в
     противофазе с одноимённой ногой (`rig.js:91-92`), это верно, логику
     пар не трогать, только сам механизм сгиба сустава внутри пары.
2.5. **Без лица.** `drawEyes()` (`rig.js:196-209`) убирается из вызова в
     `drawBodyOnly` (`rig.js:380-391`) — голова заливается кругом и всё.
     Саму функцию `drawEyes` можно удалить целиком, если она больше нигде
     не используется — мёртвый код не оставлять.
2.6. **Атака (`attack` mode) — вне объёма.** Наклон и сгиб в замахе уже
     частично есть (`swing`, `weaponAngle`) и приняты в ТЗ №16 (критерий
     8, удар виден). Трогать только если после общей смены механики
     сустава замах перестаёт читаться — тогда чинить точечно, не как
     основную задачу.

## 3. ВАЛИДАЦИЯ ПОДХОДА (сделано в этой сессии, вне боевого кода)

Формула из п.2.1 проверена в изолированном тестовом стенде
(`stick_variants_test/rig_variants.js`, функция `drawVariant2Walk` —
**не** `rig.js`, боевой код не тронут). Раскадровка цикла шага на 6 фаз —
`walk_strip.png`, видео цикла — `тз17_сустав_вместо_сноса.mp4/webm` (оба
в `ВЫДАЧА/отчёты/задача3_варианты_человечков/`). На раскадровке видно:
колено реально сгибается в фазе выноса (не просто съезжает вбок), корпус
держит наклон весь цикл, лицо отсутствует. Стоимость подхода по коду —
один `sin`/`cos`/`sqrt` на конечность на кадр, той же сложности, что и
нынешний расчёт — просадки по производительности не ожидается (критерий
9 ниже остаётся тем же порогом, что и в ТЗ №15/16, не смягчается).

## 4. РЕШЕНО ЗА ИСПОЛНИТЕЛЯ

1. Точные числа (угол наклона, амплитуда сгиба, частота шага) — тюнинг на
   глаз при приёмке, в ТЗ не фиксируются.
2. Реализация — на прежнем месте (`computeRig` + `drawLimbs` в `rig.js`),
   контракт функции модуля не меняется, `main.js` о смене метода не знает.
3. Если для тюнинга понадобятся новые числовые параметры — в
   `balance.json`, ни одного нового числа в коде (правило game4/CLAUDE.md).
4. Роли (боец/стрелок/щит), пропы, базы, тема — не трогать, вне объёма.

## 5. КРИТЕРИИ ГОТОВНОСТИ

1. Колено и локоть видимо сгибаются в течение цикла шага — угол между
   сегментами конечности меняется заметно между крайними фазами, не
   держится константным.
1а. Локоть гнётся К ТУЛОВИЩУ (острый угол сустава смотрит в сторону
    корпуса/спины), НЕ наружу-вверх от тела. Колено гнётся вперёд, по
    направлению наклона корпуса. Проверять на глаз по раскадровке —
    у прототипа (раздел 3) это видно по силуэту руки в фазах t=1.16 и
    t=4.65.
2. Стопа не скользит по земле в фазе опоры (визуально нога толкается, а
   не тащится).
3. Корпус на ходу наклонён вперёд — угол отличим от вертикали на глаз,
   не «условно есть, но не видно».
4. Голова — сплошная заливка на всех кадрах, лица нет ни на одной роли.
5. FPS не ниже 55 на 1920 при 24 юнитах (порог из ТЗ №15/16 п.9, не
   смягчается).
6. Регрессия: существующие тесты (`label_clip_check.py` и другие из ТЗ
   №15/16) дают те же исходы.
7. Удар по-прежнему виден (ТЗ №16, критерий 8), если логика атаки не
   менялась намеренно.

## 6. ТОЧКА ВХОДА ОСНОВАТЕЛЯ (P-29)

После первого переноса в РЕАЛЬНЫЙ `rig.js` (не в песочнице) — **СТОП**.
В `ВЫДАЧУ` кладётся скриншот/видео боевого экрана с новой походкой.
Работа не продолжается до вердикта. Прецедент ТЗ №15/16: два визуальных
отказа подряд на этом же rig — третьего себе не позволяем.

## 7. ВНЕ ОБЪЁМА

Механика, баланс, новые пропы/роли, площадки, реклама, звук. Атака — вне
объёма, если явно не потребуется по итогам приёмки (п.2.6).

Оценка: 2–3 часа на перенос формулы в `rig.js` и приёмку.
