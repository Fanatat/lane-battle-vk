# CLAUDE.md — lane-battler (game4)

## Первое, что нужно знать (правка 2026-09-04 — раздел ниже был написан на
## старте прототипа 25.08 и с тех пор устарел настолько, что вводил в
## заблуждение: SDK/build.py/локализация УЖЕ существуют, читайте актуально)

- **Ветка-источник:** `prototype/battle`. Активный трек разработки студии
  (единственный, см. `docs/00_ПАНЕЛЬ.txt`), не заброшенный прототип.
- **Площадки:** Яндекс Игры и VK — ОБА подключены. `platform.js`/SDK,
  `build.py` (раздельные сборки на площадку), реальная RU/EN локализация
  (`i18n.js`, `Platform.getLang()`), сейв через SDK — всё уже есть и
  боевое, не заглушки. Текущее состояние площадок/подачи — см.
  `BLOCKERS.md` (последние номера пунктов) и `docs/07_ДОЛГИ.md`.
- **Команда запуска (dev, без сборки):** `python3 -m http.server 8000` в
  корне папки. Для проверки СОБРАННОГО билда — `python3 build.py` (и/или
  `build.py vk`), см. `tests/acceptance_build.py` за примером.
- **Тесты:** `tests/` — Playwright/Python acceptance-скрипты
  (`acceptance_build.py`, `acceptance_platform.py`, `acceptance_ads*.py`,
  `fps_check.py`, `readability_check.py` и др.) — гоняются перед сдачей
  ТЗ, не только читаются. Билд-счётчики (`*_build_number.txt`) тесты
  бампают физически — вернуть `git checkout -- *_build_number.txt` после
  прогона, если это не настоящий релизный билд.
- **Боевые промо-ассеты:** есть (`ВЫДАЧА/отчёты/промо_ТЗ12`,
  `промо_ТЗ13` и билды в `dist/`), но обложка (A-06) НЕ снята — блокер:
  имя игры не подтверждено основателем (BLOCKERS, «плейсхолдер "Битва
  линий"»).

## Стек

Vanilla JS + Canvas 2D. Без движков, без Phaser, без сборщиков рантайма
(build.py — свой скрипт, не сборщик модулей). Контент и числа —
данными (`balance.json`), не в коде.

## Открытая тема — художественная проработка (BLOCKERS, «ТЗ №20... живой
## QA основателя»)

Основатель отметил живым QA нехватку художественной проработки (базы/
башни/дальность стрельбы «на тяп-ляп») и неясность ретеншн-крючков за
пределами кампании. Это НЕ додумывается самостоятельно — решение по
объёму и приоритету за основателем, зафиксировано как открытый вопрос,
не как задача к исполнению.

**Новая система входит только ВМЕСТО старой, а не поверх.** Если по ходу работы кажется,
что нужна ещё одна система, экран, валюта или файл, которых нет в ТЗ — остановиться и
спросить основателя. Остановка с вопросом поощряется, самостоятельное расширение — нет.

## Правила работы (шрамы студии, дорого купленные)

- Одна сессия = одна папка. Из этой папки не выходить даже на чтение диагностики.
- Начало сессии — `git status` на чужие изменения ДО первой правки.
- Сессия закрывается коммитом или явным отказом от изменений. WIP не хранить в `git stash`.
- **Все числа — в `balance.json`, ни одного числа в коде.** Это не стиль, это условие
  тюнинга: баланс правится ползунком, а не поиском по исходникам.
- На перерисовываемых экранах обработчики через `onclick` (перезаписывается),
  НЕ `addEventListener` — иначе копятся дубли и клик срабатывает многократно.
- Пулинг объектов обязателен: `new` в игровом цикле не вызывается.
- Приёмка — живым запуском в браузере с кликами, не чтением кода. «Файл существует» ≠ «готово».
- Автотест не заменяет взгляд на скриншот: на каждый новый экран — скриншот в отчёт.
- Скрипт обязан падать громко на несостоявшейся операции, а не продолжать молча.
- Успешный прогон — вывод в одну строку. Провал — точечно: что, где, ожидалось / получено.

## Отчётность

Файлы, предназначенные человеку, кладутся в `ВЫДАЧА/` в корне папки (подпапки `отчёты/`
и `билды/`, плоско), исключённую от коммита через `.git/info/exclude`.
Полный путь к отчёту печатается последней строкой ответа.

Отчёт, который нужен основателю глазами, отдаётся ФАЙЛОМ, а не путём в ветке.

## База знаний студии (правка 31.08 — раньше была не видна отсюда, теперь видна)

Свод лежит в `/home/vps_home_1/projects/game_studio/docs/` (не в этой папке — копий
здесь НЕТ и не заводится, P-28/Z-14). Читать по абсолютному пути:
`01_СТАНДАРТЫ.txt` (коды P/N + полный индекс), `01B_ИСПОЛНЕНИЕ.txt` (коды K…Z —
код, SDK, реклама, тесты, git), `00_ПАНЕЛЬ.txt`, `02_ЖУРНАЛ.txt`,
`АНАЛИЗ_отзывов_лидер_lane_battler.md` (законы ROADMAP.md выведены из него).
Если ТЗ ссылается на ДОКУМЕНТ ПЛОЩАДКИ (требования, лицензия), которого нет ни
здесь, ни в `docs/`, — остановиться и спросить (M-14), это первоисточник, а не
свод студии, и по памяти его не восстановить.
