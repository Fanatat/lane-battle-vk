# ТЗ №02 — Балансовый проход «дефицит и давление»

**Папка:** та же, ветка `prototype/battle` (продолжение ТЗ №01).
**Срок:** несколько часов. Кода почти нет — это правка данных плюс тестовая обвязка.

## 0. Что показала живая приёмка ТЗ №01

Основатель сыграл пять матчей: пять побед, ни одного поражения. Новизны хватило на один
матч. На вопрос «что пытался сделать руками и не смог» — ответ отрицательный: импульса
не возникло ни разу.

Три причины, все три числовые:
1. Еда постоянно упирается в потолок (90/90 на кадрах) — дефицита нет, значит выбора нет,
   значит игрок просто спамит самого дорогого юнита. Так и было.
2. База игрока 500 HP против 260 у врага — проиграть невозможно в принципе.
3. Расписание волн конечное: враг заканчивается, и бой доживает на пустом поле.
   Это дефект постановки ТЗ №01, не исполнения.

Единственный интересный момент за пять матчей случился, когда основатель отвлёкся и
вернулся к почти проигранной позиции. **Задача этого ТЗ — сделать такие моменты
закономерными, а не случайными.**

## 1. Границы

Ничего нового не добавлять. НЕ трогать: цвета и обводку юнитов, портретную раскладку,
juice (хит-стоп, тряска, цифры урона), кнопку скорости, попапы, панель отладки как
интерфейс. Никаких новых систем, экранов, валют и типов юнитов.

Меняются: числа в `balance.json`, расписание волн, и появляется тестовая обвязка в `tests/`.

## 2. Целевые числа

### 2.1 Базы

Обе базы — **300 HP**. Симметрия обязательна: игрок не должен быть прочнее противника.

### 2.2 Еда

| Параметр | Значение |
|---|---|
| Потолок | **45** (чуть выше цены C) |
| Производство | **2.2 / с** (пустая полоса → полная за ~20 с) |
| Цены | A = 8, B = 18, C = 40 (не меняются) |

Смысл потолка: банковать двух дорогих юнитов нельзя. Полоса обязана быть полной редко —
только когда игрок сознательно копит.

### 2.3 Паритет ценности юнитов — главная правка

Сейчас C доминирует: основатель спамил только его. У игры-лидера ровно эта жалоба
(«третий тип по сути тяжёлый первый, от первых толку нет»).

**Правило: боевая ценность юнита пропорциональна его цене, отклонение не более ±15%.**
Боевая ценность считается как `HP × DPS` (`DPS = урон / интервал атаки`).

Различие между типами — не в силе, а в РОЛИ и ТЕМПЕ:

| Тип | Роль | Как задаётся |
|---|---|---|
| A | затычка «прямо сейчас» | мало HP, частые слабые удары, **быстрый** ход |
| B | середина | сбалансирован по всем осям |
| C | таран «через 18 секунд» | много HP, редкие тяжёлые удары, **медленный** ход |

Скорость передвижения — отдельная ось и важная: A доезжает до бреши быстро, C доедет
позже, но пробьёт. Именно это делает выбор осмысленным при равной ценности за еду.

### 2.4 Расписание волн врага — «прилив и отлив»

Расписание продлевается до **240 секунд** и не заканчивается раньше.

| Окно | Плотность |
|---|---|
| 0–25 с | одиночный A каждые 6 с — игрок обязан выигрывать первые стычки |
| 25–60 с | A и B каждые 7 с, иногда по два |
| 60–100 с | по 3 юнита каждые 8 с, первый C около 70-й секунды |
| 100–140 с | по 4 юнита каждые 8 с, доля C растёт |
| 140–240 с | по 4–5 каждые 7 с, C преобладает |

**Поверх этого — штурмы на 40-й, 75-й и 110-й секунде: волна удвоенного размера.**
Это точки, где база игрока обязана просесть, и именно они создают почти-поражение.

## 3. Тестовая обвязка (в `tests/`, часть репозитория)

Прогон симуляции без браузера, три бота:

- **greedy** — тратит еду, как только хватает на самого дорогого доступного;
- **spamA** — спамит только A;
- **spamC** — копит и спамит только C;
- **idle** — не делает ничего.

Каждый прогон печатает: исход, длительность, минимальный HP базы игрока, долю времени с
полной полосой еды, число спавнов по типам.

### Критерии приёмки (прогон валится, если не выполнено)

| Проверка | Порог |
|---|---|
| `greedy` побеждает | 100–130 с |
| `idle` проигрывает | обязательно |
| `spamC` не строго лучший | его время не лучше `greedy` более чем на 10% |
| `spamA` тоже жизнеспособен | побеждает или доводит базу врага ниже 25% |
| Доля времени с полной полосой еды у `greedy` | **< 15%** |
| Минимальный HP базы игрока у `greedy` | **< 60%** хотя бы раз за матч |

Последние два — формализованные «дефицит» и «напряжение». Если они не выполняются,
числа подобраны неверно, сколько бы красиво ни выглядел бой.

Числа из раздела 2 — отправная точка. Если критерии не сходятся, подбирать разрешено
и нужно, но **правило паритета ценности (2.3) и симметрия баз (2.1) не нарушаются** —
это решения, а не производные величины. Итоговые числа перечислить в отчёте явно.

## 4. Отчёт

`ВЫДАЧА/отчёты/ТЗ02_отчёт.md`: таблица прогонов всех четырёх ботов, итоговый
`balance.json` целиком, и одна строка — что пришлось подвинуть относительно раздела 2
и почему.

Скриншот не требуется: визуальная часть в этом ТЗ не менялась.
