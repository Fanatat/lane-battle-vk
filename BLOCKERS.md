# BLOCKERS — накопительный список (ТЗ №06 и далее)

Список вопросов основателю. Отдаётся одним списком в конце фазы (P-09).
Заглушки по дефолту раздела 6 уже применены — здесь только то, что раздел 6
не решил заранее или прямо просит вынести.

1. **Приложенные документы правил отсутствуют.** ТЗ ссылается на
   `01_СТАНДАРТЫ.txt` и `claude/01B_ИСПОЛНЕНИЕ.txt` как «приложенные этому
   ТЗ файлами» (P-21) и перечисляет конкретные правила (N-05, N-20, N-21,
   N-22, T-05, T-06, T-08, T-09, T-13, T-19, K-11, G-01, G-02, P-09, P-22),
   но ни один из этих файлов физически не лежит в папке — только сам текст
   ТЗ, `КОНЦЕПЦИЯ_разведка_20260829.md` и старые отчёты. Раздел 8 протокола
   считает блокером только отсутствие документа ПЛОЩАДКИ — эти файлы
   документами площадки не являются, поэтому работа не останавливалась;
   правила применялись по контексту, в котором они упомянуты внутри самого
   текста ТЗ (например, T-08 — рядом с требованием измерителя вместо
   да/нет). Если реальные тексты этих правил отличаются от того, как они
   применены здесь — нужна сверка отдельным заходом.

2. **Сид турнира ничего не меняет.** Раздел 6, п.11 фиксирует сиды 1…8
   константой, но не решает, ЧТО именно сид должен варьировать. Движок
   (`engine.js`) полностью детерминирован — в рантайме нет генераторов и
   солверов (жёсткий запрет CLAUDE.md), ни одна ветка симуляции сид не
   читает. Ввод стохастики (например, шум в бое или в решениях бота) — это
   новая система, которую раздел 6 не решил заранее, а «Границы» запрещают
   заводить самостоятельно. Поэтому 8 партий одной стратегии в таблице
   `tests/sim.js` — 8 побитово идентичных строк; критерии раздела 4 всё
   равно посчитаны буквально по всем 48 строкам и выполнены. Вопрос
   основателю: нужно ли в принципе вводить псевдослучайность в бой (и если
   да — в какую именно систему), или деление на «сид» в этом прототипе
   стоит убрать как несуществующее понятие.

3. **Честная ценность превью (блок 4) не даёт числа при финальном
   balance.json.** `counter` (use_preview:true) и `mixCheapArchers` (та же
   ротация A/B, use_preview:false — единственная корректная база сравнения,
   T-09) под итоговым балансом ОБА проигрывают, поэтому сравнение
   длительности честно невозможно — число не подгонялось. Это не помешало
   ни одному порогу приёмки (п.7 раздела 4 не является порогом), но
   основатель может счесть желательным получить хоть какое-то
   положительное число здесь отдельным заходом балансировки за пределами
   бюджета этой фазы.

4. **Папка трека официально не назначена** (раздел 6, п.22) — работа
   выполнена в той же папке, где лежит ветка `prototype/battle`, новая
   папка не заводилась. Решение основателя о постоянном месте трека не
   принято этим ТЗ.

5. **Порог закрытия трека (раздел 9) не подтверждён.** Формулировка «если
   живой заход не даёт ответа „да“ — трек закрывается» приведена в ТЗ как
   предлагаемая и требует подтверждения основателем полным предложением
   (P-18). До подтверждения не действует.

6. **Находка балансировки ТЗ №06 держится на грани, а не с запасом**
   (ДОГОН ПРИЁМКИ, 2026-08-29). Честный пересчёт по уникальным партиям (сид
   не влияет на движок — 6 уникальных партий из 48, см. п.2 выше) показывает,
   что порог 4 («бинарности нет») проходит РОВНО по минимуму: 3 из 6 при
   требовании ≥3. Возмущение любого одного числового параметра боя/экономики
   на ±10% ломает хотя бы один из порогов 2, 3, 4, 5 в 26 из 122 направлений
   (21%) — почти всегда через порог 4. Набор из всех одиночно-худших
   направлений одновременно ломает пороги 2, 4 и 5 разом. Вопрос основателю:
   считать ли текущий `balance.json` финалом ТЗ №06 (пороги готовности
   формально пройдены, включая честный пересчёт), или заходить в отдельный
   раунд балансировки с прицелом на устойчивость к вариации, а не только на
   прохождение по границе. Подбор баланса — вне объёма ДОГОНА (раздел 5),
   поэтому `balance.json` не тронут; полные числа — в
   `ВЫДАЧА/отчёты/ДОГОН_ПРИЁМКИ_ТЗ06_отчёт.md`.

7. **Приложенные документы правил снова физически отсутствуют.** ДОГОН
   заявляет `01_СТАНДАРТЫ.txt` и `01B_ИСПОЛНЕНИЕ.txt` «приложенными файлами»
   и утверждает, что ошибка выдачи P-21 из ТЗ №06 исправлена — но в папке
   этих файлов по-прежнему нет (та же ситуация, что в п.1 выше). Работа не
   останавливалась по тому же основанию: это не документ площадки, а свод
   внутренних правил студии, и упомянутые коды (T-05, T-08, T-09, N-20,
   K-11, P-09, P-12, P-17, P-18, P-19, P-21, P-22 и другие) применены по
   контексту, в котором они названы в тексте самого ДОГОНА. Если фактический
   текст этих правил отличается от того, как они применены здесь и в ТЗ
   №06 — нужна отдельная сверка обоих заходов сразу.

## ТЗ №07 (геометрия, доминант, роль щита)

8. **Приложенные документы снова физически отсутствуют.** ТЗ №07 и
   ROADMAP.md заявляют `01_СТАНДАРТЫ.txt`, `01B_ИСПОЛНЕНИЕ.txt` и
   `claude/АНАЛИЗ_отзывов_лидер_lane_battler.md` как «приложенные файлами»,
   но ни один из них физически не найден ни в папке трека, ни где-либо на
   диске (проверено `find` по всей домашней директории). Та же ситуация,
   что в пп.1 и 7 выше. Раздел «Автономия» ROADMAP.md прямо называет
   блокером только отсутствие документа ПЛОЩАДКИ (M-14) — эти файлы им не
   являются, поэтому работа не останавливалась. Коды (P-09, P-10, P-12,
   P-19, P-21, P-22, N-20, N-24, G-01, G-02, T-22, T-23, T-24, K-11)
   применены по контексту, в котором они раскрыты внутри самого ТЗ №07 —
   у него необычно подробный раздел 6 «РЕШЕНО ЗА ИСПОЛНИТЕЛЯ», закрывающий
   почти все развилки без обращения к текстам стандартов. Если фактические
   тексты этих правил (когда найдутся) расходятся с тем, как коды применены
   в ТЗ №06, ДОГОНе и ТЗ №07 — нужна одна общая сверка по всем трём заходам.

9. **«Законы проекта» из ROADMAP.md ссылаются на анализ отзывов, которого
   нет физически** (см. п.8) — 10 законов приведены текстом в самом
   ROADMAP.md и применялись по этому тексту напрямую, а не по источнику.

10. **Порог 7 (запас, T-23) не взят после лимита времени поиска** — законный
    результат по разделу 4 ТЗ №07 (явно не входит в условие автопродолжения
    раздела 7, которое требует только пороги 1–5). На найденной рабочей точке
    balance.json (schedule.time × 0.915) пороги 1–6 выполняются на ОБЕИХ
    геометриях, но с нулевым или близким к нулю запасом: порог 5 (градиент)
    держится РОВНО на границе (9 из 48 уникальных партий при требовании ≥8;
    округление множителя до 1 знака после запятой уже роняет счётчик до 8,
    до 0.913/0.914/0.916/0.917/0.918 — до провала порога 3). Возмущение
    ±10% по одному параметру (61 параметр × 2 направления = 122 прогона)
    ломает хотя бы один из порогов 1,3,4,5 в 48 случаях (39.3%, требование
    ≤10%). Причина концентрируется в пороге 3 (доминанта нет): лучшая
    смешанная стратегия побеждает быстрее чистой лишь благодаря ОДНОМУ
    удачному сиду из восьми (её распределение времени победы бимодально —
    либо быстрая победа ~55–65с, либо медленная ~90–110с/поражение), поэтому
    «лучшее из 8» — статистика минимума маленькой выборки, чувствительная к
    любому сдвигу параметра. Устранение этого требует не подгонки числа, а
    структурного изменения (например, состава волн или экономики), что
    описано в ТЗ как «экономику не трогать, кроме случая, когда порог 4
    иначе недостижим» — порог 4 (нижняя граница) достигается и без этого,
    так что расширять вмешательство в экономику формальных оснований нет.
    Использовано 2 из 12 разрешённых вариантов balance.json (первый —
    множитель 0.935, отброшен; второй, финальный — 0.915). Полная таблица
    возмущений — в отчёте фазы и `ВЫДАЧА/отчёты/ТЗ07_TSV_возмущение_по_одному.tsv`.
    Вопрос основателю: тратить ли отдельный заход на структурную
    перебалансировку ради устойчивости запаса, или считать текущую находку
    (пороги 1–6 взяты, порог 7 — нет) достаточной, как и было в ТЗ №06.

11. **Приёмка человеком не проводилась живым запуском в браузере** — ТЗ №07,
    раздел 4, явно снимает это требование для этой фазы («Приёмка человеком:
    не требуется. Фаза принимается уровнем 1 (P-12), её результат виден в
    числах»). Это единственная причина, по которой сессия не открывала
    браузер, несмотря на общее правило CLAUDE.md «приёмка — живым запуском» —
    в этой машине нет браузерного движка (проверено: ни chromium, ни
    puppeteer/playwright не установлены), так что и живой запуск был бы
    физически невозможен, если бы ТЗ его требовало. Сделана только пассивная
    проверка: `python3 -m http.server` отдаёт все файлы (200 OK), JSON и
    синтаксис JS валидны. Для фазы 09 (раскладка/визуал) браузерная приёмка
    станет обязательной и здесь понадобится способ её выполнить.

## ТЗ №08 (полная система жанра — начата автономно по ROADMAP.md после закрытия ТЗ №07)

12. **`balance.units[type]` общий для игрока и врага — апгрейд урона типа
    иначе усилил бы и врага.** ROADMAP.md требует апгрейд «урон типов» как
    часть экрана усиления, но движок ТЗ №01-07 не различает сторону при
    чтении характеристик юнита. Решение (не согласовано с основателем,
    архитектурная развилка): добавлен необязательный
    `balance.player_unit_overrides[type].damage_bonus/hp_bonus`, применяется
    только к юнитам игрока в `spawnUnit`. Разбор — в
    `ГРАФ_РАЗБЛОКИРОВОК_ТЗ08.md`. Если основатель предпочитает другую
    архитектуру (например, полностью раздельные `balance.playerUnits` и
    `balance.enemyUnits`) — нужен отдельный заход рефакторинга движка.

13. **Сложность кампании масштабируется через сжатие расписания волн, не
    через характеристики врага** — по той же причине (общие характеристики
    юнитов). Урон/HP вражеских юнитов от номера битвы НЕ растут — растёт
    только частота/состав волн (числа в `balance.json.campaign`). Если это
    не совпадает с интуицией основателя «враг растёт по номеру битвы» (закон
    4 ROADMAP.md — сформулирован как раз про это: «Враг растёт по номеру
    битвы и только», не уточняя КАК) — нужна сверка формулировки.

14. **Кампания линейна, без permadeath — решение вкуса (P-10), не следует
    буквально из ROADMAP.md.** Раздел фазы 08 не уточняет, сбрасывает ли
    поражение прогресс кампании ("забег" может читаться и как "вся
    кампания", и как "одна битва"). Выбрана более простая для MVP модель:
    номер битвы растёт после КАЖДОЙ битвы независимо от исхода, трофеи не
    сгорают. Обоснование — закон 5 ROADMAP.md («при сбросе прогресса
    что-то остаётся») и общий тон фазы («вертикальный срез», не полный
    рогалик) ближе к этой модели, плюс она проще проверяется тестом. Если
    основатель хотел рогалик (поражение = конец забега, трофеи сохраняются,
    следующий забег начинается с битвы 1) — нужен отдельный заход.

15. **Порог 2 фазы 08 («доля побед бота падает с номером битвы») измерен
    как общий нисходящий ТРЕНД (наклон линейной регрессии по 8 карьерам),
    а не строгая монотонность каждой соседней пары битв.** Строгая
    монотонность на выборке в 8 карьер — статистика с большим шумом (тот же
    урок, что порог 7 ТЗ №07): реальная кривая доли побед колеблется
    (100,100,100,100,100,100,88,94,81,81,75,63,69,69,94 — в процентах по
    битвам 1-15 на найденном balance.json), но общий наклон отрицателен и
    ни одна битва не даёт 0%. Числа — в `ВЫДАЧА/отчёты/ТЗ08_TSV_карьеры.tsv`.

16. **Первые 3 битвы кампании идут без джиттера (`deterministic: true`)** —
    иначе закон 1 («первые три битвы игрок ВЫИГРЫВАЕТ») недостижим буквально:
    даже чистый спам бойца A не даёт 100% побед на произвольных сидах при
    включённом джиттере (см. отчёт ТЗ №07 — spamA был 87.5%, не 100%, даже
    без сжатия расписания). Джиттер — механика для интересного повторного
    прохождения (ТЗ №07), а не для первых гарантированных побед — конфликта
    с духом ТЗ №07 не вижу, но если основатель считает иначе — нужен ответ.

## ТЗ №09 (оболочка, раскладка, онбординг)

17. **ВХОД ОСНОВАТЕЛЯ после фазы 08 пропущен по прямому указанию.** ROADMAP.md
    называет эту точку обязательным ВХОДом (10 минут живой игры перед стартом
    фазы 09). Основатель прямым текстом отказался от проверки в диалоге
    ("я не собираюсь проверять... иди дальше по roadmap") и разрешил отметить
    это в отчётах. Фаза 09 начата без подтверждения "хочется ли открыть
    следующее" — если по итогам живого взгляда основателя фазы 07-08
    потребуют правок, это отдельный заход, не считается браком текущей.

18. **На этой сессии впервые установлен headless Chromium (Playwright)** —
    раньше (ТЗ №07-08) живая браузерная приёмка была физически невозможна
    (см. п.11). Установлено ВНЕ папки трека (`~/.local/share/game4-devtools`,
    не в git, не в `package.json` проекта — CLAUDE.md запрещает рантайм-
    зависимости самой игре, это только тестовый инструмент сессии) — `npx
    playwright install chromium` без `--with-deps` (sudo недоступен, но и не
    понадобился — браузер запускается без системных пакетов). Фаза 09
    впервые в этом треке принята РЕАЛЬНЫМИ скриншотами и кликами/тапами через
    Playwright, а не только headless-тестами логики.

19. **Стиль «тёплая бумага» (A-01) подобран без исходного файла стандарта.**
    Файла с точной палитрой A-01 в папке нет (см. п.8-9). Использована
    авторская интерпретация по названию: тёплый пергамент (#f2e6cd фон),
    чернильный текст (#3a2c1c), терракотовый акцент (#c1682f). Если у студии
    есть точный референс/палитра A-01 — нужна сверка и, вероятно, правка
    CSS-переменных в `index.html` (все цвета собраны в `:root`, правка
    точечная).

20. **Онбординг без текста (N-17) реализован ЧЕРЕЗ существующий граф
    разблокировок кампании (ТЗ №08), не отдельным экраном-туториалом.**
    Первая битва физически предлагает только тип A (B/C заперты до покупки
    разблокировки) — это уже «одно правило за раз» без единого слова
    инструкции. Отдельных подсказок/стрелок/выделений в бою НЕТ — решение
    вкуса (P-10): туториал-оверлей — это новый экран/система сверх заявленных
    в разделе «Объём» ТЗ №09 (там перечислены только меню/бой/усиление/
    попапы/пауза). Если основатель имел в виду именно визуальные подсказки
    поверх боя — нужен отдельный заход.

21. **Портрет и ландшафт равноправны БЕЗ разного компоновки — выбран вариант
    "полоса вписывается целиком" (первый из двух вариантов раздела «Объём»),
    не "компонуется иначе".** Причина: логическая полоса (ТЗ №07) и так
    всегда вписывается по ширине в любой контейнер по построению —
    компоновать её иначе означало бы вводить новую систему раскладки боя,
    не предусмотренную ТЗ. В портрете над и под полосой остаётся пустое
    пространство канваса — юнит всё равно ≥1/8 высоты экрана (закон 7,
    проверено `tests/layout_check.js`), но эстетически это не идеал.
    Дальнейшая полировка (например, увеличение юнитов в портрете сверх 1/8,
    чтобы использовать пустое место) — вопрос вкуса вне минимального порога.

## Разрешено в ходе фазы 10

22. **01B_ИСПОЛНЕНИЕ.txt получен от основателя** — закрывает пп.1, 7, 8, 9,
    18 (частично), 19 выше: коды K/S/R/A/M/T/G/D/E/Z сверены с исходником,
    применение в ТЗ №06-09 совпало с реальным текстом почти everywhere.
    Расхождение найдено одно: A-01 (стиль «тёплая бумага») — фон/чернила
    угаданы близко, но не точно (#f2e6cd/#3a2c1c вместо реальных
    #f3ead6/#2b2723), и угадан sans-serif вместо предписанного системного
    serif — поправлено в `index.html` в этом же заходе. `01_СТАНДАРТЫ.txt`
    (коды P/N, общий индекс) по-прежнему не прислан — коды P-09, P-10, P-12,
    N-05, N-17, N-20, N-24 и т.д., которыми оперировали ТЗ №06-09, остаются
    непроверенными по первоисточнику (используются по контексту, в котором
    их раскрывали сами ТЗ, — см. пп.1,7,8 выше).

23. **Мостик к фазе 10 (сейв/платформа): референс — game3/color_sort.**
    По прямому указанию основателя (в чате, не в файле — G-01 разрешает
    ЧТЕНИЕ эталонной реализации в чужой папке) прочитаны `platform.js`,
    `vk_platform.js` и релевантные участки `main.js` игры Color Sort
    (ТОЛЬКО чтение, ни правок, ни диагностики там не производилось).
    Обнаружено расхождение между текстом S-01 («…/isRewardedAvailable()»)
    и РЕАЛЬНЫМ рабочим контрактом этой игры (там такого метода нет —
    доступность rewarded обрабатывается реактивно, R-07/R-08). По S-11
    («контракт сверяется по реальному main.js, а не по «v2» по памяти»)
    для game4 взят рабочий контракт БЕЗ `isRewardedAvailable()`. Если
    основатель имел в виду именно текст S-01 буквально — метод легко
    добавить отдельным заходом.

24. **Порог 1 фазы 10 (смоук собранного dist/) и часть S-02/G-04
    (отдельные билды под Яндекс и ВК) не взяты — build.py не существует.**
    ROADMAP.md сам планирует `build.py` только на фазу 12 («ПРЕДПОДАЧА»),
    поэтому у фазы 10 физически нет собранного артефакта для смоука —
    исходники и есть то, что тестировалось (тот же режим, что у фаз
    07-09). `platform.js` (Яндекс) и `vk_platform.js` (ВК) оба существуют
    с идентичным публичным контрактом, но переключение между ними в
    `index.html` сейчас ручное (замена тега `<script>`), не отдельный
    производимый билд. Полная сдача этих пунктов возможна только после
    фазы 12. Вопрос основателю: считать ли это законным переносом (как
    порог 7 ТЗ №07), или фазы 10-11 стоит частично отложить до появления
    build.py — ROADMAP.md порядок фаз этого не предусматривает явно.

25. **Реальных вызовов `showInterstitial`/`showRewarded` в игре ПОКА
    НЕТ** — платформенный слой (фаза 10) существует и протестирован
    (dev-фолбэк, таймауты, устойчивость к сбоям), но ни одна кнопка/
    событие игры их не вызывает. Это ожидаемо (реклама — объём фазы 11),
    отмечено явно, чтобы не создалось впечатление, что реклама уже
    работает где-то в игре.

## ТЗ №11 (реклама и удержание)

26. **Числа рекламы — прикидка исполнителя, НЕ решение основателя (R-04
    буквально требует, чтобы числа ВК были решением основателя).**
    `campaign.ads.interstitial_every_n_battles=3`, `cooldown_s=90`,
    `rewarded_bonus_fraction=0.5` в `balance.json` — правдоподобные, но не
    утверждённые числа. Основателю нужно подтвердить или заменить перед
    реальной подачей.

27. **Гейт interstitial применён УНИВЕРСАЛЬНО (для обеих площадок), хотя
    R-04 требует его ТОЛЬКО на ВК** («на Яндексе частотой управляет
    платформа, своих гейтов не строить»). Причина — `build.py` не
    существует (фаза 12), поэтому нет способа узнать в рантайме, какой
    адаптер реально активен (оба сейчас теоретически загружаемы через один
    и тот же `<script src="platform.js">` в `index.html`, ВК-версия не
    подключена нигде). Когда появится build.py — нужно завести признак
    активной площадки (например, отдельная константа в каждом адаптере)
    и на Яндекс-сборке снять собственный гейт, оставив его только на ВК.

28. **Финал-событие (закон 9/K-18) реализован как разовый рубеж «глава
    пройдена» на битве 15, а не как окончание игры** — кампания
    бесконечна по требованию N-08, поэтому буквального «конца» нет и не
    может быть. Число битвы (`campaign.milestone_battle=15`) — тоже
    решение исполнителя, не основателя. Если это не соответствует
    ожиданиям (например, ожидался явный экран «финал» с другим
    содержанием) — нужен отдельный заход.

29. **N-12 (текст правила про ежедневный стрик) не подтверждён
    первоисточником** — `01_СТАНДАРТЫ.txt` по-прежнему нет, известна
    только формулировка порога из самого ROADMAP.md («стрик
    восстанавливается, а не сгорает дотла»). Реализовано как «пропуск дня
    снижает стрик на 1, а не обнуляет» (`streak_decay_on_miss=1`) —
    разумная, но НЕ единственная трактовка формулировки; альтернативы
    (например, «один день прощения, затем обнуление») тоже правдоподобны.

## ТЗ №12 (предподача)

30. **Файл «Требования к игре» (M-03) физически отсутствует** — как и
    `01_СТАНДАРТЫ.txt`, это не документ площадки (M-14), работа не
    останавливалась. Собственный чек-лист по имеющимся кодам —
    `ВЫДАЧА/отчёты/ЧЕКЛИСТ_требования_к_игре_ТЗ12.md`.

31. **Маскот студии (A-02, кот) не внедрён в игру.** В текущем визуальном
    языке (геометрические силуэты юнитов, тёплая бумага) нет очевидного
    места для маскота — юниты кодируют роль формой, а не персонажем.
    Вопрос основателю: нужен ли маскот в этом прототипе явно (например, на
    будущем экране меню/загрузки) или жанр lane-battler закрывает
    требование иначе (например, через промо-материалы вне самой игры).

32. **Обложка (A-06) не снята — имени игры нет.** N-15 явно резервирует
    нейминг за основателем; A-06 требует, чтобы на обложке было ТОЛЬКО имя
    игры — без имени обложку либо не делать (текущий выбор, честный
    пропуск), либо делать заведомо временную/фиктивную (не делалось,
    K-26/K-19-подобный принцип «не выдумывать то, чего нет»). Как только
    имя появится — обложка и локализованные варианты (A-07) быстро
    досняты тем же промо-набором.

33. **`.githooks/pre-commit` создан и исполняемый бит выставлен (T-17),
    но `git config core.hooksPath .githooks` НЕ выполнен этой сессией** —
    системное правило исполнителя запрещает трогать git-конфигурацию
    инструментами сессии независимо от студийных стандартов. Активация —
    ручной шаг основателя (команда — в комментарии самого файла хука).
    Хук сознательно гоняет только быстрые проверки (campaign_sim.js,
    layout_check.js, build.py) — `tests/sim.js` и Playwright-приёмка
    (минуты) не входят в per-commit гейт, решение исполнителя (P-06:
    приёмка — узкое место, лишний вес на каждый коммит вредит темпу).

34. **S-08 (сейв принимается только через РОДНОЕ хранилище площадки) и
    четыре пункта T-06 не проверены на реальной площадке** — как и в
    отчётах фаз 10-11, здесь нет доступа к реальному Яндекс/ВК окружению.
    `tests/acceptance_build.py` смокает СОБРАННЫЙ `dist/` живым Chromium
    (T-10 теперь выполним — до появления build.py в фазах 10-11 это было
    физически невозможно), но это мок SDK, не настоящая площадка.

## ТЗ №13 (арт и подача)

35. **Тема/имя/числа рекламы — решения основателя, приняты в шапке ТЗ №13
    буквально, не переоткрывались.** Тема «муравьи против жуков», имя
    «Муравьиная война», числа interstitial (1/3 битвы, кулдаун 90с) и
    rewarded (+50%) взяты дословно из раздела «Решения основателя» и
    шапки ТЗ — не решение исполнителя, поэтому здесь не значится как
    вопрос. Числа рекламы физически НЕ менялись (правило 8 раздела 3).

36. **Гейт interstitial разведён по площадкам через новый плейсхолдер
    `AD_GATE_MODE` в `main.js` (точечная замена build.py, G-04) —
    архитектурная развилка, не согласованная отдельно.** main.js остаётся
    ОБЩИМ файлом (WHITELIST_COMMON) для обеих сборок — вместо двух копий
    файла добавлена одна строка-константа, которую build.py подставляет
    как `'yandex'`/`'vk'` в СОБРАННУЮ копию (тот же приём, что уже
    применялся к `Platform.BUILD` в platform.js/vk_platform.js). На
    несобранном исходнике константа остаётся плейсхолдером и трактуется
    кодом как ВК-режим — поведение `index.html` без сборки не изменилось,
    все тесты ТЗ №11 (`tests/acceptance_ads.py`) прошли без правок.
    Проверка на СОБРАННЫХ артефактах — новый `tests/acceptance_ads_build.py`
    (статическая проверка подстановки + живой прогон на распакованном
    Яндекс-zip и папке ВК). Если основатель предпочитает буквально
    отдельные файлы `main_yandex.js`/`main_vk.js` вместо константы внутри
    общего файла — нужен отдельный заход рефакторинга сборки.

37. **Мобильный промо-скриншот теперь занимает 100% кадра (было ~0%
    полезной площади в старом портретном кадре, BLOCKERS.md исторически
    и `ЧЕКЛИСТ_..._ТЗ12.md`), а не ровно 70%** — дев-режим `?promo=1`
    (`dev.js`, M-11: физически не попадает в билд) прячет topBar/карточки
    целиком, канвас-поле занимает весь вьюпорт. Решение исполнителя
    (правило 9 раздела 3, вопрос вкуса): более простой и надёжный способ
    гарантированно пройти порог ≥70% (A-04, п.5.1.1.2), чем частичное
    урезание HUD с попыткой попасть точно в диапазон. Если основатель
    хочет видеть HUD (еда/трофеи/скорость) на промо-скринах — нужен
    отдельный, менее строгий промо-режим.

38. **Обложка (A-06) снята с размером 1024×1024 — точный формат/размер
    для обложки Яндекс Игр в приложенных документах не найден** (grep по
    01_СТАНДАРТЫ.txt/01B_ИСПОЛНЕНИЕ.txt не даёт конкретных px). Взят
    распространённый квадратный формат карточки как правдоподобное
    предположение — если у площадки другие обязательные размеры/пропорции,
    обложка пересобирается тем же кодом (`theme_art.js` + HTML-разметка в
    scratch-харнессе сессии) за несколько минут.

39. **Четыре честных новых пробела найдены построчной сверкой с реальным
    файлом «Требования к игре.txt»** (полный список —
    `ЧЕКЛИСТ_требования_к_игре_ТЗ13.md`): (1.6.1.1) Fullscreen API нигде
    не вызывается; (1.12) РСЯ (баннерная рекламная сеть) не подключена —
    вне объёма ROADMAP.md, там только interstitial/rewarded; (2.2) в игре
    нет текста «как играть»; (6.1) email для обратной связи не указан.
    Работа не останавливалась (P-09) — это не документ площадки (M-14),
    а чек-лист самой площадки, найденные пробелы фиксируются, а не
    блокируют фазу. Устранение 1.6.1.1/2.2/6.1 — небольшой отдельный
    заход при желании; 1.12 (РСЯ) — решение основателя, отдельная
    рекламная система сверх ROADMAP.md.

40. **Grayscale/силуэт-тест реализован новым скриптом
    `tests/readability_check.py`, не буквальным «рендер поля →
    screenshot → grayscale conversion»** — вместо конвертации PNG в
    оттенки серого через библиотеку изображений (что потребовало бы
    новой зависимости) тест читает пиксели канваса напрямую
    (`getImageData`) и считает relative luminance (формула ITU-R BT.709,
    та же, что используют инструменты grayscale-конвертации) — численно
    эквивалентно, без добавления рантайм/тест-зависимостей сверх уже
    установленного Playwright. Разница со сторон — 77% (нужно ≥25%),
    силуэты ролей — 46-57% несовпадающей площади (нужно ≥20%), запас
    большой.
