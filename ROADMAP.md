# ROADMAP — LANE-BATTLER (рабочее имя, в интерфейс не выводится)

Версия 1 от 2026-08-30. Основание: живая приёмка ТЗ №06 дала вердикт
основателя «весело» — трек продолжается и превращается из прототипа боя в
игру для подачи на Яндекс Игры и VK.

**Как этим пользоваться.** Запрос к исполнителю сжимается до «делай фазу N
по ROADMAP» (P-07). Каждая фаза ниже — самостоятельное ТЗ: цель, объём,
машинный порог готовности, границы. Отдельного текста ТЗ на фазу не
пишется, кроме фазы 07, которая выдана расширенно как образец.

**Автономия (P-09).** Исполнитель не останавливается ради уточнений:
заглушка по дефолту, пометка `// BLOCKER:` в коде, строка в `BLOCKERS.md`,
работа продолжается. Список отдаётся одним пакетом в конце фазы.
Останавливаться обязательно только в трёх случаях: ломается то, что уже
работает у живых игроков; требуется необратимое действие; нужен документ
площадки, которого нет в папке (M-14).

**Фаза закончена — начинается следующая, без спроса.** Основатель входит
только в точках, отмеченных ниже словом ВХОД. Если порог фазы не взят
после честной попытки и лимита перебора — фаза закрывается отчётом «порог
не взят», и это законный результат; следующая фаза стартует только если
она от проваленной не зависит, иначе работа встаёт и ждёт ВХОДа.

**Предусловие, которое исполнитель не закрывает.** Репозиторий существует
в одном экземпляре, remote нет (06_ШРАМЫ, п.24). Это риск основателя.

---

## ЗАКОНЫ ПРОЕКТА

Выведены из 375 отзывов 1–4★ на игру-лидера жанра
(`claude/АНАЛИЗ_отзывов_лидер_lane_battler.md`, приложен файлом). Обязательны
для каждой фазы; нарушение закона — дефект, а не предмет вкуса.

1. Первые три битвы игрок ВЫИГРЫВАЕТ. Стартовое поражение = потерянный игрок.
2. Кнопка скорости ×1/×2/×3 с первого боя, без ограничений.
3. Проигрыш никогда не пустой: каждый забег даёт видимый прирост.
4. Усиление игрока НИКОГДА не компенсируется масштабированием врага. Враг
   растёт по номеру битвы и только.
5. При сбросе прогресса что-то остаётся. Полный ноль — самая ненавидимая
   механика лидера.
6. Числа сторон в одной размерности и честные на старте.
7. Юнит не меньше 1/8 высоты экрана, силуэт различим.
8. Реклама никогда не условие победы (и требование Яндекса п.4.5.2).
9. У игры есть финал-событие.
10. В бою должно быть решение. «Думать не нужно» — диагноз лидера и место
    для дешёвой отстройки.

Сверх законов действуют правила студии по кодам: N-01, N-05, N-06, N-07,
N-08, N-10, N-12, N-20, N-21, N-22, N-23, N-24, K-*, S-*, R-*, T-*, G-*.
Тексты — в приложенных `01_СТАНДАРТЫ.txt` и `01B_ИСПОЛНЕНИЕ.txt`.

---

## ФАЗА 07 — ГЕОМЕТРИЯ И ДОМИНАНТ
Выдана отдельным расширенным ТЗ (`ТЗ_07_ГЕОМЕТРИЯ.md`). Кратко: баланс
перестаёт зависеть от размера экрана (N-24), устраняется доминант «спам
стрелка», роль щита получает или доказательство, или новое определение,
турнир начинает мерить на двух геометриях (T-24) и на настоящей выборке
(T-22, T-23). Оценка 4–6 часов.

## ФАЗА 08 — ПОЛНАЯ СИСТЕМА ЖАНРА
**Зачем.** Сейчас в папке вертикальный срез боя. По N-05 разведочный
прототип обязан нести полную систему жанра в минимальном исполнении:
без меты между боями игра не отвечает на вопрос «вернётся ли игрок».

**Объём.** Кампания из последовательности битв с растущим номером. Между
битвами — экран усиления: постоянные апгрейды за валюту забега (доход еды,
HP базы, урон типов, разблокировка типа юнита). Раздел «ГРАФ
РАЗБЛОКИРОВОК» (N-10) пишется ДО кода: узлы — что игрок открывает, рёбра —
зависимости; разблокировка меняет ГЛАГОЛ, а не число. Кнопка скорости
×1/×2/×3. Первые три битвы гарантированно выигрышные (закон 1).

**Порог.** (1) Прогон бота-кампании проходит 15 битв подряд без ручного
вмешательства; (2) кривая сложности монотонна — доля побед бота падает с
номером битвы, но ни одна битва не даёт нулевой доли; (3) ни один апгрейд
не компенсируется ростом врага (закон 4) — проверяется тестом: при
фиксированном номере битвы усиление игрока строго повышает долю побед;
(4) проигранный забег всегда даёт ненулевой прирост валюты (закон 3);
(5) «долины боли» нет: после любой разблокировки доход не падает ниже
уровня до неё (закон 5).

**Вне объёма.** Реклама, сейв, площадки, звук, финальный арт.
Оценка 10–14 часов. **ВХОД ОСНОВАТЕЛЯ** после фазы: 10 минут, три-четыре
битвы подряд, один вопрос — «хочется ли открыть следующее».

## ФАЗА 09 — ОБОЛОЧКА, РАСКЛАДКА, ОНБОРДИНГ
**Зачем.** Фаза 07 делает баланс независимым от экрана логически, эта —
показывает игру на телефоне так, чтобы в неё можно было играть пальцем.

**Объём.** Экран меню, экран боя, экран усиления, попапы победы и
поражения, пауза. Портретная и ландшафтная раскладки как равноправные:
полоса вписывается целиком либо компонуется иначе — решение исполнителя,
критерий ниже. Тач и мышь, тап-таргеты. Онбординг без текста (N-17): первые
битвы вводят по одному правилу. Стиль «тёплая бумага» (A-01) вместо
текущей тёмной заглушки.

**Порог.** (1) Скриншоты всех экранов в трёх ширинах и в портрете (T-03,
T-04); (2) юнит не мельче 1/8 высоты экрана в портрете (закон 7) —
проверяется скриптом по метрике рендера, не глазами; (3) каждая кнопка
кликается мышью и тачем, переходы туда-обратно (K-14); (4) ни одна видимая
строка не обещает того, чего нет (K-26).

Оценка 8–10 часов. Вне объёма: реклама, покупки, сейв.

## ФАЗА 10 — СЛОЙ ПЛОЩАДКИ И СЕЙВ
**Объём.** `platform.js` по контракту студии v2 (S-01), dev-фолбэк (S-09),
init-таймаут с обработкой «платформа не умеет метод» (S-10). Объект сейва
целиком (S-03), запись по событиям (S-04), схема и функция миграции (S-05),
ограниченный сейв для бесконечной части (S-07). Отдельные билды под Яндекс
и ВК из общих исходников (S-02, G-04).

**Порог.** (1) Смоук собранного `dist/` живым браузером в обеих формах
артефакта (T-10); (2) тест «сохранил → вернулся» на объекте, который
платформа реально записала (T-07); (3) тест миграции на реальном старом
прогрессе; (4) прогон с молчащей платформой и с платформой, кидающей
исключение после init.

Оценка 6–8 часов.

## ФАЗА 11 — РЕКЛАМА И УДЕРЖАНИЕ
**Объём.** Interstitial и rewarded по R-01…R-09: вызов первой инструкцией,
кнопка rewarded не прячется, базовая функция бесплатна, награда честная.
Бесконечный источник контента после конца кампании (N-08), ежедневный
крючок возвращения (N-12, K-24), финал-событие кампании (закон 9, K-18).

**Порог.** (1) Тесты рекламы без блокировщика (T-05); (2) при недоступной
рекламе функция выдаётся бесплатно и игрок не застревает (R-02, R-07);
(3) стрик восстанавливается, а не сгорает дотла (N-12); (4) реклама
никогда не условие победы (закон 8) — проверяется guard'ом.

Оценка 8–10 часов. **ВХОД ОСНОВАТЕЛЯ** после фазы: полный билд живьём,
четыре пункта T-06, реальный показ рекламы.

## ФАЗА 12 — ПРЕДПОДАЧА
**Объём.** `build.py` с белым списком и вырезанием дев-флагов (M-11, M-12),
версионирование и плашка BUILD (G-07…G-09), теги (G-10), предкоммит-хук с
закреплённым битом (T-17), сверка с файлом «Требования к игре» (M-03),
скриншоты и обложка под обе площадки (A-04…A-07), проверка на чистом
профиле (T-21).

**Порог.** (1) Сборка падает, если отладочный файл попал в архив;
(2) распаковка архива подтверждает отсутствие дев-флагов; (3) чек-лист
«Требования к игре» пройден построчно с отметками; (4) промо-набор снят по
A-09/A-10.

Оценка 6–8 часов. **ВХОД ОСНОВАТЕЛЯ**: предполётный список (P-25), подача
из кабинета — зона основателя (M-10).

---

## ЦЕНА КАРТЫ

Исполнителю 42–56 часов. Основателю — три ВХОДа (после фаз 08 и 11 и на
подаче) плюс ответы на BLOCKERS пачками раз в день. Для сравнения: модуль
удержания в августе стоил 26 ТЗ и порядка 52 заходов.

## ЧЕГО В КАРТЕ НЕТ НАМЕРЕННО

Мультиплеера, кланов, боевого пропуска, серверной части (N-18). Второй
площадки сверх Яндекса и ВК до починки удержания (N-19). Собственного арта
уровня продакшена — стиль студии закрывает потребность целиком (A-01).
Имени игры: нейминг по N-15 делается на фазе 12 решением основателя.
