# ТЗ №07 — LANE-BATTLER: ГЕОМЕТРИЯ, ДОМИНАНТ, РОЛЬ ЩИТА

Дата: 2026-08-30. Фаза 07 по `ROADMAP.md` (приложен). Ветка `prototype/battle`,
поверх коммитов ТЗ №06 и догона приёмки. Мастер-спека папки — `CLAUDE.md`.
Приложены файлами: `ROADMAP.md`, `01_СТАНДАРТЫ.txt`, `01B_ИСПОЛНЕНИЕ.txt`,
`АНАЛИЗ_отзывов_лидер_lane_battler.md` (P-21).

---

## 1. ПРОБЛЕМА

Живая приёмка ТЗ №06 дала вердикт «весело» и вскрыла три вещи, которые
турнир не мог увидеть.

**Первая и главная.** Баланс зависит от размера экрана. Один и тот же билд:
на десктопе партии 197 и 299 секунд, во второй игрок просел до 48% HP базы
и отыгрался; на телефоне — победа за 70,9 секунды без единого урона по
базе. Причина: дальность стрелка (3.5uw), дистанция подкрепления и радиус
залпа базы заданы в размерах юнита, а длина полосы зависит от ширины
экрана. На портретной раскладке эти дистанции покрывают почти всё поле, и
демпферы, ради которых делалась фаза 06, физически не успевают включиться.
Аудитория площадок преимущественно мобильная — то есть турнир принимал
игру, в которую большинство игроков не играет (N-24).

**Вторая.** Доминант виден глазами: и на десктопе, и на телефоне поле в
конце партии забито стрелками. Турнир доминанта не поймал, потому что мерил
одну геометрию.

**Третья.** Щит появляется в партиях, но его необходимость по-прежнему не
доказана ничем: A/B из ТЗ №06 порог не подтвердил.

И отдельно: движок детерминирован, поэтому турнир из 48 партий — это 6
наблюдений, размноженных восемь раз (T-22). Пока это так, ни один порог в
штуках не измеряет то, что заявляет.

## 2. ДИАГНОЗ

- Дистанции боя выражены в размерах юнита, длина полосы — в пикселях
  экрана. Любое изменение вьюпорта меняет правила игры.
- Стрелок бьёт на дистанции и не имеет слабости вблизи, поэтому его спам не
  наказывается ничем.
- Залп базы калиброван так, что щит переживает пять залпов, но игра ни разу
  не ставит игрока в положение, где без щита нельзя.
- Источника вариативности в движке нет: сид не влияет ни на что.

## 3. ОБЪЁМ РАБОТ

Блоки в порядке зависимости, после каждого коммит и прогон тестов.

### Блок 0. Инвентаризация и baseline (P-19, G-01, G-02)
`git status` до первой правки. Выписать в отчёт фактические текущие
значения: длину полосы в пикселях на десктопе и в портрете, все дистанции в
uw, HP и урон типов, цены, доход еды. Дальнейшие блоки оперируют этими
измеренными числами. `BLOCKERS.md` продолжается, не обнуляется.

### Блок 1. Логическая карта вместо пиксельной
Ввести ЛОГИЧЕСКУЮ длину полосы — константу, не зависящую от вьюпорта. Все
боевые дистанции (дальность стрелка, дистанция подкрепления, радиус залпа
базы, ширина фронта в терминах расстояния) выражаются в долях этой длины.
Рендер отвечает только за то, как логическая полоса ложится в пиксели.

Отправная точка пересчёта — нынешний ДЕСКТОПНЫЙ баланс: доли берутся такие,
чтобы на десктопной геометрии числа совпали с сегодняшними. Мобильная
геометрия после этого обязана давать ту же игру, а не другую.

### Блок 2. Источник вариативности
Ввести управляемый джиттер, от которого зависит сид: разброс таймингов
решений ИИ и порядка выбора цели в пределах, названных в разделе 6. Цель не
«сделать случайно», а получить настоящую выборку: разные сиды дают разные
траектории (T-22). Джиттер отключается флагом для отладки.

### Блок 3. Слабость стрелка вблизи
У стрелка появляется минимальная дистанция стрельбы: вплотную он
беспомощен. Это лечит доминанта механикой, а не запретом по имени (N-20), и
даёт работу бойцу и щиту. Величина — в разделе 6.

### Блок 4. Роль щита — доказать или переопределить
A/B одной стратегии с щитом и без (T-09). Если разница по доле побед не
достигает 15 процентных пунктов — щит получает свойство, которого нет ни у
кого: он ЕДИНСТВЕННЫЙ переживает залп базы, и штурм чужой базы без него
физически невозможен (усилить залп до этого порога). Повторить замер один
раз. Результат в отчёт числом в обоих случаях.

### Блок 5. Турнир на двух геометриях (T-24)
`tests/sim.js` гоняет полный турнир дважды: в десктопной и в мобильной
портретной геометрии, и печатает обе таблицы. Все пороги считаются по
уникальным траекториям (T-22) и сопровождаются проверкой устойчивости
±10% по каждому параметру (T-23).

### Блок 6. Отчёт
`ВЫДАЧА/отчёты/ТЗ07_отчёт.md`: обе таблицы, числа по каждому порогу, доля
ломающих направлений, итоговый `balance.json`, `BLOCKERS.md`.

---

## 4. КРИТЕРИЙ ГОТОВНОСТИ (машинный)

Турнир: 6 стратегий × 8 сидов × 2 геометрии = 96 партий.

1. **Выборка настоящая**: уникальных траекторий не менее 90% от числа партий.
2. **Геометрии сошлись**: медианная длительность партии в мобильной
   геометрии отличается от десктопной не более чем на 20%, и доля партий с
   нулевым уроном по базе победителя отличается не более чем на 15
   процентных пунктов.
3. **Доминанта нет** ни на одной геометрии: ни одна чистая стратегия не
   быстрее лучшей смешанной.
4. **Нижняя граница** (N-20): не менее 3 стратегий из 6 выигрывают не менее
   половины своих партий — на каждой геометрии отдельно.
5. **Градиент**: не менее 15% партий заканчиваются с min HP базы победителя
   в диапазоне 20–80%.
6. **Щит обоснован**: A/B даёт не менее 15 процентных пунктов разницы, либо
   применён дефолт-Б блока 4 и число напечатано.
7. **Запас есть** (T-23): доля направлений ±10%, ломающих любой из порогов
   3–5, не превышает 10% (в фазе 06 было 21%).
8. Тесты зелёные, вывод по K-11, `git status` чистый.

**Лимит подбора**: не более 12 вариантов `balance.json` после того, как
блоки 1–4 собраны. Порог 7 не взят после лимита — фаза закрывается отчётом,
это законный результат, а не провал.

**Приёмка человеком**: не требуется. Фаза принимается уровнем 1 (P-12), её
результат виден в числах. Следующий вход основателя — после фазы 08.

---

## 5. ВНЕ ОБЪЁМА

Мета между боями, апгрейды, кампания — фаза 08. Раскладка, меню, онбординг,
стиль «тёплая бумага», размер юнита на экране — фаза 09. Сейв, площадки,
реклама — фазы 10–11. Кодирование стороны и типа цветом остаётся как есть до
фазы 09.

---

## 6. РЕШЕНО ЗА ИСПОЛНИТЕЛЯ (P-10)

1. **Логическая длина полосы**: 100 условных единиц. Все дистанции — в них.
2. **Пересчёт**: доли выбираются так, чтобы на десктопе воспроизвести
   нынешние uw-значения с точностью до 5%. Десктопный баланс — эталон.
3. **Мобильная геометрия для турнира**: соотношение сторон 9:16, логическая
   полоса та же. Если рендер в портрете не помещает полосу целиком — это
   проблема фазы 09, турниру достаточно логической модели.
4. **Джиттер**: ±10% к интервалу принятия решения ИИ и случайный выбор из
   равноценных целей. Экономику и урон джиттер НЕ трогает — иначе замер
   поплывёт.
5. **Джиттер игрока-бота** — тот же, симметрично.
6. **Флаг отключения джиттера**: `deterministic: true` в конфиге прогона,
   для отладки и воспроизведения бага.
7. **Минимальная дистанция стрельбы стрелка**: 40% от его дальности. Ближе —
   не стреляет и не наносит урона вовсе.
8. **Стрелок вплотную не убегает** — он остаётся на месте и терпит. Механику
   отступления не вводить, это отдельная система.
9. **Залп базы, усиление в блоке 4**: поднимать не более чем вдвое от
   нынешнего; если и этого мало — записать в BLOCKERS, щит не удалять.
10. **Сиды**: 1…8 на каждой геометрии, зашиты константой.
11. **Тайм-аут партии**: 300 с, категория `timeout`, не-победа для обеих
    сторон.
12. **Формат таблиц**: TSV, две таблицы с шапками, колонка геометрии.
13. **Уникальность траектории**: sha256 лога событий; лог — тик, актор,
    действие.
14. **Порядок работ при конфликте**: сначала геометрия (блок 1), потом
    вариативность (блок 2). Обратный порядок смешает два эффекта.
15. **Новых типов юнитов не вводить.** Три типа.
16. **Новых видимых строк не вводить.** Если понадобилась — не более двух
    слов, скриншот в отчёт.
17. **Экономику не трогать**, кроме случая, когда порог 4 иначе недостижим;
    любое изменение цены или дохода называется в отчёте отдельной строкой.
18. **Дев-панель** дополняется: текущая геометрия, логические координаты
    фронта, включён ли джиттер.
19. **Папка трека**: работать в той же, где ветка. Вопрос назначения папки
    остаётся в BLOCKERS, новую не заводить.
20. **Если после блока 1 десктопный баланс поплыл** (пороги 3–5 упали) —
    это ожидаемо: подстроить доли в пределах лимита 12 вариантов, эталоном
    считать поведение, а не буквальные числа.

---

## 7. ПРОТОКОЛ АВТОНОМИИ

По ROADMAP, раздел «Автономия». Вопросы вкуса решаются исполнителем и
показываются скриншотом. Фаза 08 стартует сразу после закрытия этой, без
ожидания ответа основателя, если пороги 1–5 взяты.

## 8. КУДА КЛАДЁТСЯ РЕЗУЛЬТАТ (P-22)

`ВЫДАЧА/отчёты/ТЗ07_отчёт.md`, `ВЫДАЧА/отчёты/скриншоты_ТЗ07/`,
`BLOCKERS.md` в корне, коммиты по блоку в `prototype/battle`.
