# ДОГОН ПРИЁМКИ К ТЗ №06 — ПРОВЕРКА ИЗМЕРЕНИЯ, НЕ ИГРЫ

Дата: 2026-08-29. Это НЕ новая фаза и НЕ правка игры. Это уровень 1
приёмки ТЗ №06 (P-12), который не может быть закрыт по текстовому отчёту.
Работа идёт в той же папке и той же ветке `prototype/battle`, поверх
последнего коммита ТЗ №06.

**Приложены файлами:** `01_СТАНДАРТЫ.txt`, `01B_ИСПОЛНЕНИЕ.txt`. В прошлый
раз ТЗ ссылалось на коды правил, а файлов в папке не было — ошибка выдачи,
исправлена (P-21). Все ссылки вида T-05, N-20 ищутся в этих двух файлах.

---

## 1. ПРОБЛЕМА

Отчёт по ТЗ №06 утверждает, что пройдены все восемь порогов, но два из них
арифметически не могли быть посчитаны так, как записаны, а третий получен
поиском по самому порогу. Проверяется не игра — проверяется измерение.

**Причина первая.** В BLOCKERS записано, что сид не влияет на движок. Пороги
4 и 5 сформулированы в партиях из 48 («не менее 19», «не менее 6»). Если сид
ничего не меняет, уникальных партий шесть, а каждая размножена восемь раз.
Тогда порог 4 мог быть закрыт тремя уникальными исходами, а порог 5 — одним.
Это ложный зелёный по T-05: посчитано честно, а величина измерена не та.

**Причина вторая.** Отчёт называет офлайн-скан ~1000 комбинаций, после
которого первый же закоммиченный `balance.json` прошёл все пороги. Формально
бюджет 12 вариантов соблюдён, фактически перебор шёл по критерию «пройти
порог» — то есть числа подобраны под тест. Устойчивость находки неизвестна:
она может быть островком шириной в один шаг.

**Причина третья.** Результаты доложены словом «пройдено» там, где порог
требовал число (пороги 2, 3, 6, 7). Приёмка уровня 1 идёт по артефакту,
не по пересказу.

## 2. ОБЪЁМ РАБОТ

### Блок Д1. Сколько партий на самом деле
Дедуп прогона: партия хешируется по её траектории (последовательность
событий боя), одинаковый хеш = одна и та же партия. `sim.js` печатает
`уникальных партий: N из 48` и помечает дубли в TSV отдельной колонкой.
Пороги 4 и 5 пересчитываются **по уникальным партиям**, с сохранением тех же
долей: не менее 40% уникальных партий с промежуточным min HP, и не менее
одной восьмой уникальных партий с камбэком (min HP ≤40% и победа).

### Блок Д2. Устоит ли находка вне выборки
Три прогона, ни один не меняет `balance.json`:
1. **Новые сиды** — 9…16 вместо 1…8.
2. **Возмущение по одному** — каждый числовой параметр боя по очереди
   ±10% от значения, остальные на месте; печатается, сколько прогонов из
   всех сохранили пороги 2, 3, 4, 5.
3. **Худший набор** — все параметры одновременно смещены в ту сторону, где
   каждый по отдельности дал результат хуже.

### Блок Д3. Числа наружу
`sim.js` печатает по каждому из восьми порогов ТЗ №06 фактическое значение
и требуемое, а не «passed». Отдельными строками: выигрыш превью в процентах
(порог 7) и разница по доле побед с типом C и без него в процентных пунктах
(порог 6).

### Блок Д4. Артефакты
В `ВЫДАЧА/отчёты/`: дополнение к отчёту ТЗ06 с результатами Д1–Д3, TSV
основного прогона целиком, TSV трёх прогонов Д2, итоговый `balance.json`,
три кадра из блока 5, `BLOCKERS.md`. Всё это отдаётся файлами в чат (P-22).

## 3. КРИТЕРИЙ ГОТОВНОСТИ

1. Напечатано число уникальных партий и пороги 4 и 5 пересчитаны по ним.
2. Три прогона Д2 выполнены, по каждому напечатано, какие пороги устояли.
3. По всем восьми порогам напечатаны факт и требование числами.
4. Артефакты Д4 существуют и отданы файлами.
5. `git status` чистый, работа закоммичена.

**Результат «пороги развалились» — это тоже готовность.** Если на новых
сидах или на возмущении пороги падают, работа на этом заканчивается и
докладывается как есть.

## 4. РЕШЕНО ЗА ИСПОЛНИТЕЛЯ

1. `balance.json` в этой работе **НЕ МЕНЯЕТСЯ НИ НА ОДИН ПАРАМЕТР**. Любая
   правка чисел здесь уничтожает смысл проверки.
2. Механика тоже не меняется. Правится только `tests/sim.js` и то, что
   нужно для замера.
3. Хеш партии: sha256 от строкового лога событий боя (тик, актор, действие).
   Если такого лога нет — собрать минимальный из существующих величин:
   тик, число живых юнитов каждой стороны, HP обеих баз.
4. Возмущению подлежат числовые параметры боя и экономики: HP, урон,
   скорости, цены, доход, кулдаун и радиус залпа, дистанция подкрепления,
   тайминги волн. Не возмущаются: геометрия поля, `front_depth` как целое,
   число типов юнитов.
5. Целочисленный параметр при ±10% округляется наружу (вверх для «+»,
   вниз для «−»), но не ниже 1.
6. Если полный перебор возмущений дороже 15 минут прогона — брать по
   одному шагу «+» и «−» на параметр, этого достаточно.
7. Сид турнира, если он действительно ни на что не влияет, **не чинится
   джиттером в этой работе**. Факт фиксируется числом уникальных партий и
   выносится в отчёт. Введение источника вариативности — предмет отдельного
   решения основателя, не эта работа.
8. Если уникальных партий оказалось 48 (то есть сид всё-таки влияет) —
   так и написать, блок Д1 закрывается этой строкой.
9. Порог 7 порогом по-прежнему не является: любое число печатается как
   есть, подгонять запрещено.
10. Если ошибка обнаружена в самом `sim.js` предыдущей фазы — чинить и
    назвать в отчёте, что именно считалось неверно и как это меняет
    результаты ТЗ №06.

## 5. ВНЕ ОБЪЁМА

Подбор баланса. Джиттер и любая новая случайность. Правки механики,
интерфейса, кодирования цвета. Портретная раскладка. Всё, что уже было вне
объёма ТЗ №06.

## 6. ПРОТОКОЛ АВТОНОМИИ

Тот же, что в ТЗ №06, раздел 8: не останавливаться, вопросы копить в
`BLOCKERS.md` одним списком. Оценка: 1–2 часа.
